import { Action } from "./../action";

function identity(value: any) {
    return value;
}

describe ("action", () => {
    it("it should be possible to increse the counter", () => {
        // const input = { counter: 5 };
        // const expectedOutput = { counter: 6 };
        // const action = new Action();
        // const output = action.increase(input, identity);
        // expect(output).toEqual(expectedOutput);
    });
});
